# Security and Privacy

Never commit API keys, precise home coordinates for a real deployment, database credentials, `.env`, or production secrets. Production deployments must configure `ADMIN_TOKEN` and `SECRET_KEY` before exposing administrative functionality.
